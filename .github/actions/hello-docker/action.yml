name: Hello world
author: Prajwal
descritption: Some description
inputs:
  who-to-greet:
    description: "who to greet"
    required: true
    default: prajwal
outputs:
  time:
    description: "the greeting time"
runs:
  using: "docker"
  image: "Dockerfile"

  args:
    - ${{ inputs.who-to-greet }}
