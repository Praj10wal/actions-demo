on: push
jobs:
  testing-action:
    runs-on: ubuntu-latest
    steps:
      
      - uses: actions-demo/hello-docker@v1
        id: hello
        with:
          who-to-greet: "world"
      - run: |
           echo "Time: ${{ steps.hello.outputs.time }}"
           echo $HELLO
